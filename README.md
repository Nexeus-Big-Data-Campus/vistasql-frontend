# VistaSQL Frontend project

A pretty cool SQL SELECT query visualizer.

## Setting up the project

1. Run `npm install`

## Running the project locally

1. Run `npm start`

2. Visit [localhost:3000](http://localhost:3000/)